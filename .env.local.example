#+#+#+#+ Local Development Environment Variables
#
# This file shows example values for local development and testing.
# Copy this file to `.env.local` to override defaults.
#
# NOTE: .env.local is in .gitignore and will NOT be committed.

# Dev-only diagnostics and tools are controlled via URL query params (not env flags):
# - Debug panel:        ?debug
# - Tracking debug:     ?debugOverlay
# - Lighting debug:     ?debugLights=1

# Tracking (ESPresense)
# Minimum confidence required to accept a device position update.
VITE_TRACKING_ESPRESENSE_MIN_CONFIDENCE=69
